#include "monty.h"

/**
 * nop - function that does not do anything
 * @stack: stack
 * @line_count: does not matter
 *
 * Return: Nothing
 */
void nop(stack_t **stack, unsigned int line_count)
{
	(void) stack;
	(void) line_count;
}
